import { Markup } from "telegraf"

//BISCEP
export const curlMartillo = async (ctx: any) => {
  return ctx.reply(`cooming soon...`,
    Markup.inlineKeyboard([
      [Markup.button.callback(`Menu ejercicios`, `menuExercises`), Markup.button.callback(`Menu principal`, `menu_principal`)]
    ]))
}
export const curlPredicador = async (ctx: any) => {
  return ctx.reply(`cooming soon...`,
    Markup.inlineKeyboard([
      [Markup.button.callback(`Menu ejercicios`, `menuExercises`), Markup.button.callback(`Menu principal`, `menu_principal`)]
    ]))
}
export const curlAlternado = async (ctx: any) => {
  return ctx.reply(`cooming soon...`,
    Markup.inlineKeyboard([
      [Markup.button.callback(`Menu ejercicios`, `menuExercises`), Markup.button.callback(`Menu principal`, `menu_principal`)]
    ]))
}
export const curlInclinado = async (ctx: any) => {
  return ctx.reply(`cooming soon...`,
    Markup.inlineKeyboard([
      [Markup.button.callback(`Menu ejercicios`, `menuExercises`), Markup.button.callback(`Menu principal`, `menu_principal`)]
    ]))
}
//TRISCEP
export const pressFrances = async (ctx: any) => {
  return ctx.reply(`cooming son...`,
    Markup.inlineKeyboard([
      [Markup.button.callback(`Menu ejercicios`, `menuExercises`), Markup.button.callback(`Menu principal`, `menu_principal`)]
    ]))
}
export const fondos = async (ctx: any) => {
  return ctx.reply(`cooming son...`,
    Markup.inlineKeyboard([
      [Markup.button.callback(`Menu ejercicios`, `menuExercises`), Markup.button.callback(`Menu principal`, `menu_principal`)]
    ]))
}
export const pushdowns = async (ctx: any) => {
  return ctx.reply(`cooming son...`,
    Markup.inlineKeyboard([
      [Markup.button.callback(`Menu ejercicios`, `menuExercises`), Markup.button.callback(`Menu principal`, `menu_principal`)]
    ]))
}
//HOMBRO
export const pressMilitar = async (ctx: any) => {
  return ctx.reply(`cooming soon...`,
    Markup.inlineKeyboard([
      [Markup.button.callback(`Menu ejercicios`, `menuExercises`), Markup.button.callback(`Menu principal`, `menu_principal`)]
    ])
  )
}
